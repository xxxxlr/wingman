using UnityEngine;

public class TextIndicator {
    public string text;
    public Vector3 point;
    public TextIndicator(string text, Vector3 point)
    {
        this.text = text;
        this.point = point;
    }
}
